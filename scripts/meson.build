scripts = files('deck.connectors.lua', 'deck.widgets.lua')

install_data(
    scripts,
    install_dir: get_option('datadir') / 'deck-assistant',
    install_tag: 'bin',
    install_mode: 'rw-r--r--',
)
